{
    "name": "jam-session",
    "version": "1.0.0",
    "description": "Jam Session capstone project",

    "scripts": {
        "build": "npm --prefix ./frontend install && npm run --prefix ./frontend build && cp -r ./frontend/build ./backend/build"
    },
    "engines": {
        "node": "14.x"
    }
}
