{
    "name": "jam-session",
    "version": "1.0.0",
    "description": "Jam Session capstone project",
    "scripts": {
        "build": "npm run --prefix ./frontend build"
    },
    "engines": {
        "node": "14.x"
    },
    "dependencies": {
        "react-scripts": "^5.0.1"
    }
}
